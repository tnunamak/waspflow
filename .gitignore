# waspflow runtime state lives under $WASPFLOW_HOME (default ~/.local/state),
# not in the repo. Nothing runtime should land here, but guard anyway.
*.log
/tmp/
.DS_Store

# crash dumps
core.*
node_modules/
ui/node_modules/

# Local ds (devspecs) task-slice state — agent workflow, never product
devspecs/
